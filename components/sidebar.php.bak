<?php /* Professional Sidebar Component */ ?>
<style>
    /* ===== PROFESSIONAL SIDEBAR STYLES ===== */
    .sidebar {
        position: fixed;
        left: 0;
        top: 70px;
        height: calc(100vh - 70px);
        width: 260px;
        background: linear-gradient(135deg, rgba(255, 255, 255, 0.95) 0%, rgba(249, 247, 244, 0.95) 100%);
        border-right: 1px solid rgba(78, 31, 0, 0.1);
        padding: 0;
        overflow-y: auto;
        overflow-x: hidden;
        z-index: 1000;
        transition: all 0.3s ease;
        box-shadow: 2px 0 12px rgba(0, 0, 0, 0.05);
    }

    /* Dark Mode */
    body.dark-mode .sidebar {
        background: linear-gradient(135deg, rgba(45, 52, 54, 0.98) 0%, rgba(58, 63, 68, 0.98) 100%);
        border-right-color: rgba(169, 124, 80, 0.15);
    }

    /* Sidebar Logo Section */
    .sidebar-logo {
        padding: 1.5rem;
        border-bottom: 2px solid rgba(78, 31, 0, 0.08);
        display: flex;
        align-items: center;
        gap: 0.75rem;
        background: rgba(78, 31, 0, 0.02);
        margin-top: 70px;
    }

    body.dark-mode .sidebar-logo {
        border-bottom-color: rgba(169, 124, 80, 0.15);
        background: rgba(169, 124, 80, 0.05);
    }

    .sidebar-logo-icon {
        font-size: 1.8rem;
        line-height: 1;
    }

    .sidebar-logo-text {
        font-weight: 700;
        color: #4e1f00;
        font-size: 0.9rem;
        letter-spacing: 0.5px;
        line-height: 1.2;
    }

    body.dark-mode .sidebar-logo-text {
        color: #a97c50;
    }

    /* Navigation Section */
    .sidebar-nav {
        padding: 1.5rem 0;
        padding-bottom: 200px;
    }

    .sidebar-nav-label {
        padding: 0.5rem 1.5rem;
        font-size: 0.75rem;
        font-weight: 700;
        color: #999;
        text-transform: uppercase;
        letter-spacing: 1px;
        margin-top: 1rem;
        margin-bottom: 0.5rem;
    }

    body.dark-mode .sidebar-nav-label {
        color: #888;
    }

    .sidebar-nav-label:first-child {
        margin-top: 0;
    }

    /* Navigation Links */
    .sidebar-link {
        display: flex;
        align-items: center;
        gap: 1rem;
        padding: 0.85rem 1.5rem;
        color: #495057;
        text-decoration: none;
        transition: all 0.3s ease;
        border-left: 3px solid transparent;
        font-size: 0.95rem;
        font-weight: 500;
        position: relative;
    }

    .sidebar-link:hover {
        background: rgba(78, 31, 0, 0.08);
        color: #4e1f00;
        border-left-color: #4e1f00;
    }

    .sidebar-link.active {
        background: linear-gradient(to right, rgba(78, 31, 0, 0.12) 0%, rgba(78, 31, 0, 0.04) 100%);
        color: #4e1f00;
        border-left-color: #4e1f00;
        font-weight: 600;
    }

    .sidebar-link.active::before {
        content: '';
        position: absolute;
        left: 0;
        top: 0;
        bottom: 0;
        width: 3px;
        background: #4e1f00;
    }

    body.dark-mode .sidebar-link {
        color: #bbb;
    }

    body.dark-mode .sidebar-link:hover {
        background: rgba(169, 124, 80, 0.15);
        color: #a97c50;
        border-left-color: #a97c50;
    }

    body.dark-mode .sidebar-link.active {
        background: linear-gradient(to right, rgba(169, 124, 80, 0.2) 0%, rgba(169, 124, 80, 0.08) 100%);
        color: #a97c50;
        border-left-color: #a97c50;
    }

    .sidebar-link-icon {
        font-size: 1.15rem;
        width: 24px;
        text-align: center;
        flex-shrink: 0;
    }

    .sidebar-link-text {
        flex: 1;
    }

    .sidebar-link-badge {
        background: #ff5252;
        color: white;
        border-radius: 12px;
        padding: 0.2rem 0.5rem;
        font-size: 0.75rem;
        font-weight: 700;
        flex-shrink: 0;
    }

    /* Sidebar Footer */
    .sidebar-footer {
        position: absolute;
        bottom: 0;
        left: 0;
        right: 0;
        padding: 1.5rem;
        border-top: 2px solid rgba(78, 31, 0, 0.08);
        background: rgba(78, 31, 0, 0.02);
    }

    body.dark-mode .sidebar-footer {
        border-top-color: rgba(169, 124, 80, 0.15);
        background: rgba(169, 124, 80, 0.05);
    }

    /* User Card */
    .user-card {
        margin-bottom: 1rem;
        padding: 1rem;
        background: #fff;
        border: 1px solid rgba(78, 31, 0, 0.08);
        border-radius: 10px;
        transition: all 0.3s ease;
    }

    .user-card:hover {
        border-color: #4e1f00;
        box-shadow: 0 3px 12px rgba(78, 31, 0, 0.1);
    }

    body.dark-mode .user-card {
        background: #3a3f44;
        border-color: rgba(169, 124, 80, 0.15);
    }

    body.dark-mode .user-card:hover {
        border-color: #a97c50;
        box-shadow: 0 3px 12px rgba(169, 124, 80, 0.15);
    }

    .user-avatar {
        width: 50px;
        height: 50px;
        border-radius: 50%;
        object-fit: cover;
        border: 2px solid #4e1f00;
        margin-bottom: 0.75rem;
        transition: all 0.3s ease;
    }

    .user-card:hover .user-avatar {
        border-color: #a97c50;
        box-shadow: 0 2px 8px rgba(78, 31, 0, 0.15);
    }

    body.dark-mode .user-avatar {
        border-color: #a97c50;
    }

    .user-name {
        font-weight: 700;
        color: #2d3436;
        font-size: 0.9rem;
        margin-bottom: 0.25rem;
    }

    body.dark-mode .user-name {
        color: #fff;
    }

    .user-email {
        font-size: 0.75rem;
        color: #999;
        word-break: break-word;
    }

    body.dark-mode .user-email {
        color: #a0a0a0;
    }

    .user-role {
        display: inline-block;
        margin-top: 0.5rem;
        padding: 0.25rem 0.75rem;
        background: #4e1f00;
        color: white;
        border-radius: 20px;
        font-size: 0.7rem;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 0.5px;
    }

    body.dark-mode .user-role {
        background: #a97c50;
    }

    /* Mode Toggle & Logout Buttons */
    .sidebar-controls {
        display: flex;
        gap: 0.75rem;
    }

    .sidebar-btn {
        flex: 1;
        padding: 0.7rem;
        border: 1px solid rgba(78, 31, 0, 0.15);
        background: #fff;
        color: #4e1f00;
        border-radius: 8px;
        cursor: pointer;
        font-weight: 600;
        transition: all 0.3s ease;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 0.5rem;
        font-size: 0.85rem;
    }

    .sidebar-btn:hover {
        background: #4e1f00;
        color: white;
        border-color: #4e1f00;
    }

    body.dark-mode .sidebar-btn {
        background: #3a3f44;
        border-color: rgba(169, 124, 80, 0.2);
        color: #a97c50;
    }

    body.dark-mode .sidebar-btn:hover {
        background: #a97c50;
        color: white;
        border-color: #a97c50;
    }

    .sidebar-btn.logout-btn:hover {
        background: #ff5252;
        border-color: #ff5252;
    }

    body.dark-mode .sidebar-btn.logout-btn:hover {
        background: #ff8a80;
        border-color: #ff8a80;
    }

    /* Close Button (Mobile) */
    .sidebar-close {
        position: absolute;
        top: 1rem;
        right: 1rem;
        background: rgba(78, 31, 0, 0.1);
        border: none;
        width: 36px;
        height: 36px;
        border-radius: 50%;
        color: #4e1f00;
        cursor: pointer;
        font-size: 1.2rem;
        display: none;
        place-items: center;
        transition: all 0.3s ease;
        z-index: 100;
    }

    .sidebar-close:hover {
        background: rgba(78, 31, 0, 0.2);
    }

    body.dark-mode .sidebar-close {
        background: rgba(169, 124, 80, 0.15);
        color: #a97c50;
    }

    body.dark-mode .sidebar-close:hover {
        background: rgba(169, 124, 80, 0.25);
    }

    /* Scrollbar Styling */
    .sidebar::-webkit-scrollbar {
        width: 6px;
    }

    .sidebar::-webkit-scrollbar-track {
        background: rgba(78, 31, 0, 0.05);
    }

    .sidebar::-webkit-scrollbar-thumb {
        background: rgba(78, 31, 0, 0.2);
        border-radius: 3px;
    }

    .sidebar::-webkit-scrollbar-thumb:hover {
        background: rgba(78, 31, 0, 0.3);
    }

    body.dark-mode .sidebar::-webkit-scrollbar-track {
        background: rgba(169, 124, 80, 0.08);
    }

    body.dark-mode .sidebar::-webkit-scrollbar-thumb {
        background: rgba(169, 124, 80, 0.2);
    }

    body.dark-mode .sidebar::-webkit-scrollbar-thumb:hover {
        background: rgba(169, 124, 80, 0.3);
    }

    /* Responsive Design */
    @media (max-width: 900px) {
        .sidebar {
            transform: translateX(-100%);
            box-shadow: 2px 0 20px rgba(0, 0, 0, 0.2);
        }

        .sidebar.show {
            transform: translateX(0);
        }

        .sidebar-close {
            display: grid;
        }
    }

    /* Main content spacing */
    body {
        padding-left: 260px;
        padding-top: 70px;
    }

    @media (max-width: 900px) {
        body {
            padding-left: 0;
            padding-top: 70px;
        }
    }
</style>

<aside class="sidebar" id="sidebar">
    <button class="sidebar-close" id="sidebar-close" aria-label="Close sidebar"><i class="bi bi-x-lg"></i></button>

    <!-- Logo -->
    <div class="sidebar-logo">
        <span class="sidebar-logo-icon">✈️</span>
        <span class="sidebar-logo-text">Fayyaz<br>Travels CRM</span>
    </div>

    <!-- Navigation -->
    <nav class="sidebar-nav">
        <!-- Main Section -->
        <div class="sidebar-nav-label">Main</div>
        <a href="dashboard.php"
            class="sidebar-link <?php echo basename($_SERVER['PHP_SELF']) === 'dashboard.php' ? 'active' : ''; ?>">
            <i class="bi bi-house-door sidebar-link-icon"></i>
            <span class="sidebar-link-text">Dashboard</span>
        </a>

        <!-- Tickets Section -->
        <div class="sidebar-nav-label">Tickets</div>
        <a href="tickets.php"
            class="sidebar-link <?php echo basename($_SERVER['PHP_SELF']) === 'tickets.php' ? 'active' : ''; ?>">
            <i class="bi bi-ticket-perforated sidebar-link-icon"></i>
            <span class="sidebar-link-text">All Tickets</span>
            <span class="sidebar-link-badge" id="ticket-badge" style="display: none;">0</span>
        </a>
        <a href="create-ticket.php"
            class="sidebar-link <?php echo basename($_SERVER['PHP_SELF']) === 'create-ticket.php' ? 'active' : ''; ?>">
            <i class="bi bi-plus-circle sidebar-link-icon"></i>
            <span class="sidebar-link-text">Create Ticket</span>
        </a>

        <!-- Communication -->
        <div class="sidebar-nav-label">Communication</div>
        <a href="messages.php"
            class="sidebar-link <?php echo basename($_SERVER['PHP_SELF']) === 'messages.php' ? 'active' : ''; ?>">
            <i class="bi bi-chat-dots sidebar-link-icon"></i>
            <span class="sidebar-link-text">Messages</span>
            <span class="sidebar-link-badge" id="message-badge" style="display: none;">0</span>
        </a>
        <a href="notifications.php"
            class="sidebar-link <?php echo basename($_SERVER['PHP_SELF']) === 'notifications.php' ? 'active' : ''; ?>">
            <i class="bi bi-bell sidebar-link-icon"></i>
            <span class="sidebar-link-text">Notifications</span>
            <span class="sidebar-link-badge" id="notification-badge" style="display: none;">0</span>
        </a>

        <!-- Other Section -->
        <div class="sidebar-nav-label">Other</div>
        <a href="timeline.php"
            class="sidebar-link <?php echo basename($_SERVER['PHP_SELF']) === 'timeline.php' ? 'active' : ''; ?>">
            <i class="bi bi-clock-history sidebar-link-icon"></i>
            <span class="sidebar-link-text">Timeline</span>
        </a>
    </nav>

    <!-- Footer with User Info and Controls -->
    <div class="sidebar-footer">
        <!-- User Card -->
        <div class="user-card">
            <img src="<?php echo htmlspecialchars($user['picture'] ?? 'assets/images/default-avatar.png'); ?>"
                alt="<?php echo htmlspecialchars($user['name'] ?? 'User'); ?>" class="user-avatar">
            <div class="user-name"><?php echo htmlspecialchars($user['name'] ?? 'Guest User'); ?></div>
            <div class="user-email"><?php echo htmlspecialchars($user['email'] ?? 'user@example.com'); ?></div>
            <?php if (!($is_admin ?? false)): ?>
                <span class="user-role">Employee</span>
            <?php else: ?>
                <span class="user-role">Admin</span>
            <?php endif; ?>
        </div>

        <!-- Controls -->
        <div class="sidebar-controls">
            <button class="sidebar-btn" id="mode-toggle-btn" onclick="toggleDarkMode()">
                <i class="bi bi-moon-stars" id="mode-icon"></i>
            </button>
            <a href="logout.php" class="sidebar-btn logout-btn" title="Logout">
                <i class="bi bi-box-arrow-left"></i>
            </a>
        </div>
    </div>
</aside>

<script>
    // Update mode icon when toggled
    const updateModeIcon = () => {
        const icon = document.getElementById('mode-icon');
        if (icon) {
            if (document.body.classList.contains('dark-mode')) {
                icon.className = 'bi bi-sun';
            } else {
                icon.className = 'bi bi-moon-stars';
            }
        }
    };

    // Patch toggleDarkMode to update icon
    const origToggleDarkMode = window.toggleDarkMode;
    window.toggleDarkMode = function () {
        if (origToggleDarkMode) origToggleDarkMode();
        updateModeIcon();
    };

    // Close sidebar on mobile when clicking outside
    const sidebar = document.getElementById('sidebar');
    const closeBtn = document.getElementById('sidebar-close');

    if (closeBtn) {
        closeBtn.addEventListener('click', closeSidebar);
    }

    document.addEventListener('click', (e) => {
        if (window.innerWidth <= 900 && sidebar && sidebar.classList.contains('show')) {
            if (!sidebar.contains(e.target) && !e.target.classList.contains('sidebar-hamburger')) {
                closeSidebar();
            }
        }
    });

    // Control sidebar visibility on resize
    window.addEventListener('resize', () => {
        if (window.innerWidth > 900 && sidebar) {
            sidebar.classList.remove('show');
        }
    });

    function toggleSidebar() {
        const sidebar = document.getElementById('sidebar');
        if (sidebar) {
            sidebar.classList.toggle('show');
        }
    }

    function closeSidebar() {
        const sidebar = document.getElementById('sidebar');
        if (sidebar) {
            sidebar.classList.remove('show');
        }
    }

    function toggleSidebarMobile() {
        toggleSidebar();
    }

    function toggleNavbarDropdown() {
        const dropdown = document.getElementById('navbar-dropdown');
        if (dropdown) {
            dropdown.classList.toggle('show');
        }
    }

    // Update mode icon on page load
    document.addEventListener('DOMContentLoaded', updateModeIcon);
</script>